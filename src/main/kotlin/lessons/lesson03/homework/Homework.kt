package org.example.lessons.lesson03.homework

class Homework {

    // 1. Название мероприятия (неизменяемая публичная переменная известная на момент написания)
    val name: String = "Monster Hackaton"

    // 2. Дата проведения (публичная переменная известная на момент написания, но может быть изменена из-за переноса)
    var date: String = "10 ноября 2025"

    // 4. Подробный бюджет мероприятия, включая расходы на оборудование, кейтеринг и другие операционные расходы.
    // (приватная неизменяемая переменная - её не должны увидеть и бюджет уже запланирован и утверждён и не должен изменяться)
    private val budget: String = "подробная информация с перечислением поставщиков и сумм для внутреннего пользования"

    // 5. Количество участников (публичная изменяемая переменная — участники могут регистрироваться/отменяться)
    var participantsCount: Int = 150

    // 6. Длительность хакатона (публичная изменяемая переменная — может быть скорректирована)
    var durationHours: Int = 48

    // 7. Контактная информация и условия соглашений с поставщиками (приватная изменяемая — данные могут обновляться, но не для публики)
    private var supplierAgreements: String = "контакты и условия работы с поставщиками"

    // 8. Текущее состояние хакатона (публичная изменяемая переменная — статус меняется по ходу мероприятия)
    var status: String = "подготовка"

    // 9. Список спонсоров (публичная изменяемая переменная — спонсоры могут добавляться)
    var sponsorsList: String = "Спонсор А, Спонсор Б, Спонсор В"

    // 10. Бюджет мероприятия (приватная изменяемая переменная — общая цифра бюджета, может корректироваться)
    private var budgetEvent: Int = 5000000

    // 11. Текущий уровень доступа к интернету (публичная изменяемая переменная — может меняться)
    var internetAccessLevel: Int = 100

    // 12. Информация о транспортировке оборудования и координации команд поддержки (приватная изменяемая — внутренняя логистика)
    private var logisticsInfo: String = "детали транспортировки и координации"

    // 13. Количество команд (публичная изменяемая переменная — команды могут регистрироваться)
    var teamsCount: Int = 30

    // 14. Перечень задач (публичная изменяемая переменная — задачи могут добавляться/убираться)
    var tasksList: String = "Задача 1, Задача 2, Задача 3"

    // 15. План эвакуации (публичная неизменяемая переменная — план утверждён и не меняется)
    val evacuationPlan: String = "План эвакуации: выход через главные двери..."

    // 16. Список доступного оборудования (публичная изменяемая переменная — оборудование может выдаваться/возвращаться)
    var availableEquipment: String = "Ноутбуки, мониторы, кабели"

    // 17. Список свободного оборудования (публичная изменяемая переменная — меняется в реальном времени)
    var freeEquipment: String = "2 монитора, 5 кабелей HDMI"

    // 18. График питания участников (зависит от поставщика питания, определяемого за неделю до начала) (публичная изменяемая переменная с отложенной инициализацией, так как на момент создания поставщик и график не известны)
    lateinit var mealSchedule: String

    // 19. План мероприятий на случай сбоев (публичная неизменяемая переменная — план утверждён заранее)
    val contingencyPlan: String = "План действий при сбоях: резервные серверы, генераторы..."

    // 20. Список экспертов и жюри (публичная изменяемая переменная — состав может уточняться)
    var expertsAndJudges: String = "Эксперт 1, Эксперт 2, Судья 1"

    // 21. Политика конфиденциальности (публичная неизменяемая переменная — документ утверждён)
    val privacyPolicy: String = "Текст политики конфиденциальности"

    // 22. Приватные отзывы участников и зрителей (приватная изменяемая переменная — для внутреннего анализа)
    private var privateFeedback: String = "Сбор отзывов для анализа проблем"

    // 23. Текущая температура в помещении (публичная изменяемая переменная — меняется в реальном времени)
    var currentTemperature: Int = 22

    // 24. Данные мониторинга и анализ производительности сетевого оборудования (приватная изменяемая — для отладки)
    private var networkMonitoringData: String = "Данные мониторинга сети"

    // 25. Регулятор скорости интернета (публичная изменяемая переменная — коэффициент может меняться)
    var internetSpeedFactor: Double = 0.5

    // 26. Уровень освещения (публичная изменяемая переменная — может регулироваться)
    var lightingLevel: String = "средний"

    // 27. Лог событий мероприятия (приватная изменяемая переменная — внутренний лог)
    private var eventLog: String = "Лог событий мероприятия"

    // 28. Возможность получения медицинской помощи (публичная изменяемая переменная — да/нет, может меняться)
    var medicalHelpAvailable: Boolean = true

    // 29. Планы и процедуры для обеспечения безопасности (публичная неизменяемая переменная — утверждены заранее)
    val safetyProcedures: String = "Протоколы безопасности и чрезвычайных ситуаций"

    // 30. Регистрационный номер мероприятия (публичная неизменяемая переменная — присвоен и не меняется)
    val eventRegistrationNumber: Int = 956314785

    // 31. Максимально допустимый уровень шума (публичная неизменяемая переменная — норматив)
    val maxNoiseLevel: Int = 80

    // 32. Индикатор превышения уровня шума (публичная изменяемая переменная — превышен/не превышен в данный момент)
    var isNoiseLevelExceeded: Boolean = false

    // 33. План взаимодействия с прессой (публичная изменяемая переменная — может корректироваться)
    var pressPlan: String = "План работы с прессой"

    // 34. Детальная публичная информация о проектах каждой команды (публичная изменяемая переменная,
    // ресурсоёмкие вычисления — компиляция и статистика; используем lateinit, т.к. данные появятся после прогонов)
    lateinit var teamsProjectsInfo: String

    // 35. Статус получения всех необходимых разрешений (публичная изменяемая переменная — статус меняется по мере получения)
    var permitsStatus: String = "в процессе получения"

    // 36. Индикатор доступности специализированного оборудования (публичная изменяемая переменная — доступно/недоступно в данный момент)
    var isSpecialEquipmentAvailable: Boolean = true

    // 37. Список партнеров мероприятия (публичная изменяемая переменная — партнёры могут добавляться)
    var partnersList: String = "Партнёр 1, Партнёр 2"

    // 38. Отчет с фото, видео и отзывами (публичная переменная, генерируется после завершения —
    // вычисляется один раз при первом обращении, используем by lazy)
    val report: String by lazy { "Отчет: фото, видео, отзывы" }

    // 39. Внутренние правила распределения призов (приватная неизменяемая переменная — утверждены заранее)
    private val prizeDistributionRules: String = "Внутренние правила распределения призов"

    // 40. Список разыгрываемых призов (публичная изменяемая переменная — призы могут добавляться)
    val prizesList: String = "Приз 1, Приз 2, Приз 3"

    // 41. Контактная информация экстренных служб (приватная неизменяемая переменная — для внутреннего пользования)
    private val emergencyContacts: String = "Контакты экстренных служб и медперсонала"

    // 42. Особые условия для участников с ограниченными возможностями (публичная неизменяемая переменная — утверждены заранее)
    val accessibilityConditions: String = "Особые условия для участников с ОВЗ"

    // 43. Общее настроение участников (публичная изменяемая переменная — определяется опросами, может меняться)
    var participantsMood: String = "позитивное"

    // 44. Подробный план хакатона (публичная переменная с отложенной инициализацией — инициализируется перед началом, зависит от состава команд)
    lateinit var detailedPlan: String

    // 45. Имя знаменитого специального гостя (публичная переменная с отложенной инициализацией — объявляется за день до мероприятия)
    lateinit var specialGuestName: String

    // 46. Максимальное количество людей, которое может вместить место проведения.
    val maxCountPeopleVenue: Int = 350

    // 47. Количество часов, отведенное каждой команде для работы над проектом.
    val countHoursWorkingProject: Int = 24

}